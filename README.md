# DAI-Assignment11
This project extracts structured data from professor profiles using NER, cleans and consolidates entities, and builds a NetworkX knowledge graph. It also implements an LLM-based evaluator that scores student answers by comparing them to reference responses.
